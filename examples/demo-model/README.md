# demo-model

A tiny, dependency-free stand-in for a Hugging Face model directory.
Use it to try ModelLedger locally without downloading anything:

    model-ledger manifest --local-dir examples/demo-model